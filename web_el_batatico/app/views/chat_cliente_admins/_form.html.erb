<%= form_with(model: chat_cliente_admin, local: true) do |form| %>
  <% if chat_cliente_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chat_cliente_admin.errors.count, "error") %> prohibited this chat_cliente_admin from being saved:</h2>

      <ul>
      <% chat_cliente_admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :mensaje %>
    <%= form.text_field :mensaje, id: :chat_cliente_admin_mensaje %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.date_select :fecha, id: :chat_cliente_admin_fecha %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
